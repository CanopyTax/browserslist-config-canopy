{
  "name": "browserslist-config-canopy",
  "version": "1.0.0",
  "description": "browsers list config for canopy",
  "main": "src/browserslist-config-canopy.js",
  "repository": "git@github.com:CanopyTax/browserslist-config-canopy.git",
  "author": "Joel Denning and Jeff Green",
  "license": "Apache-2.0",
  "scripts": {
    "test": "jest --config jest.json",
    "watch-tests": "jest --config jest.json --watch"
  },
  "devDependencies": {
    "browserslist": "^3.1.1",
    "jest": "^22.4.2"
  }
}
